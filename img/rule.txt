Add new Image
1. on adding new item , item controller will create a new hash of
key (item name + chef)

2. we will have two image size as of now lg and md
so put image with name <hash>.png either in lg or md folder

Updating image with new one
1. rename new image name with old one

Image lookup:

1. once we have all the items we will have hash key of image and we know what
size of image is req. eg. hash =abcd size= md

image path will be abcd_md.png

all files will be in .png format